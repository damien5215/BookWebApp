@model List<BookShared.Models.Book>

@{
    ViewBag.Title = "Products";
}

<h2>Products</h2>

<div>

    @*@foreach (var book in Model)
    {
        <p>
            @book.Author.Name - @book.Title - @book.Img

            <img src="@book.Img" width="100" height="160">

        </p>
    }*@

    @foreach (var book in Model)
    {
        <div class="container">
            <br><hr><br>
            <div class="row">
                <div class="col-md-2">
                    <img src="@book.Img" width="100" height="160">
                </div>
                <div class="col-md-10">
                    <p>@book.Author.Name</p>
                    <p>@book.Title</p>
                    <p>@book.Description</p>
                    @*<p>@book.Genres</p>*@
                </div>
            </div>
            @*<br><hr>*@
        </div>
    }

</div>